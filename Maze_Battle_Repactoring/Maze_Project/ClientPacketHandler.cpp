#include "pch.h"
#include "ClientPacketHandler.h"

bool ClientPacketHandler::HandlerRoomCreate(BYTE* buffer, uint32 len, SessionRef ref)
{
	return false;
}

bool ClientPacketHandler::HandlerLogin(BYTE* buffer, uint32 len, SessionRef ref)
{
	return false;
}
